{% extends '::backend.html.twig' %}

{% block body -%}
    <div id="content" class="span10">
			<ul class="breadcrumb">
				<li>
					<i class="icon-home"></i>
					<a href="#">CMS</a>
					<i class="icon-angle-right"></i>
				</li>
				<li><a href="#">Posts</a></li>
			</ul>

			<div class="row-fluid sortable">
				<div class="box span12">
					<div class="box-header" data-original-title>
						<h2><i class="halflings-icon user"></i><span class="break"></span>List of posts</h2>
						<div class="box-icon">
							<a href="#" class="btn-setting"><i class="halflings-icon wrench"></i></a>
							<a href="#" class="btn-minimize"><i class="halflings-icon chevron-up"></i></a>
							<a href="#" class="btn-close"><i class="halflings-icon remove"></i></a>
						</div>
					</div>
					<div class="box-content">
						<table class="table table-striped table-bordered bootstrap-datatable datatable">
						  <thead>
							  <tr>
								  <th>Id</th>
								  <th>Title</th>
								  <th>Abstract (short intro)</th>
								  <th>Category</th>
								  <th>Date</th>
								  <th>Actions</th>
							  </tr>
						  </thead>
						  <tbody>
						  {% for entity in entities|reverse(true) %}
							<tr>
								<td>{{ entity.id }}</td>
								<td class="center">{{ entity.title }}</td>
								<td class="center">{{ entity.abstract|slice(0,30) }}...</td>
								<td class="center">{{ entity.category }}</td>
								<td class="center">{{ entity.updated|date('d-m-Y H:i:s')}}</td>
								<td class="center">
									<a class="btn btn-success" href="{{ path('cms_pageeng_show', { 'id': entity.id }) }}">
										<i class="halflings-icon white zoom-in"></i>
									</a>
									<a class="btn btn-info" href="{{ path('cms_pageeng_edit', { 'id': entity.id }) }}">
										<i class="halflings-icon white edit"></i>
									</a>
								</td>
							</tr>
						  {% endfor %}
						  </tbody>
						  <br>
						  <a class="btn btn-success" href="{{ path('cms_pageeng_new') }}">
						  <i class="halflings-icon white pencil"></i>Create new
						  </a>
						  <br>
					  </table>
					</div>
				</div><!--/span-->
			</div><!--/row-->
	</div><!--/.fluid-container-->
			<!-- end: Content -->
		</div><!--/#content.span10-->
{% endblock %}
