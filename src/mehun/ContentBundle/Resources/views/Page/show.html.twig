{% extends '::base.html.twig' %}

{% block body -%}
    <!-- Breadcrumbs
================================================== -->
    <div class="page relative">
        <div class="page_layout page_margin_top clearfix">
            <div class="page_header clearfix">
                <div class="page_header_left">
                    <h1 class="page_title">{{ entity.title }}</h1>
                    <ul class="bread_crumb">
                        <li> <a href="{{ path('mehun_content_homepage') }}"> Naslovna </a> </li>
                        <li class="separator icon_small_arrow right_gray"> &nbsp; </li>
                        <li> {{ entity.title }} </li>
                    </ul>
                </div>
            </div>

            <!-- Tekst
            ================================================== -->
            <div>
                <div class="page_left page_margin_top">
                    <div class="column_left">
                        <h4>Kategorija: {{ entity.category.name }}</h4><br>
                        <h5>Objavljeno: {{ entity.updated|date('d-m-Y H:i:s') }}</h5>
                        {{ entity.content|raw }}
                    </div>
                </div>
            </div>

            <!-- Kontakt info box
            ================================================== -->
            <div class="page_right page_margin_top">
                <ul>
                    <li class="home_box green">
                        <h2> <a href="{{ path('kontakt') }}" title="Kontakt"> Lokacija i kontakt </a> </h2>
                        <div class="news clearfix">
                            <p class="text"> Ivana Mažuranića 70, 42000 Varaždin<br>
                                Tel.: +385 42 240 366<br>
                                Mob.: +385 98 267 140<br>
                                Fax: +385 42 204 860</p>
                        </div>
                    <li class="sidebar_box">
                        <ul class="contact_data">
                            <li class="clearfix"> <span class="social_icon mail"></span>
                                <p class="value"> e-mail: info@mehun.hr </p>
                            </li>
                            <li class="clearfix"> <span class="social_icon form"></span>
                                <p class="value"> Pošaljite upit putem<a href="{{ path('kontakt') }}"> kontakt obrasca</a> </p>
                            </li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </div>
{% endblock %}
